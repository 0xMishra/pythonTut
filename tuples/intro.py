"""
heterogenous, immutable, paranthesis
all the methods in list will be useful in tuple
except those that mutates the data
"""

a = (1,2,3) # can also be written as a = 1,2,3
b =() # empty tuple
print(type(a))
