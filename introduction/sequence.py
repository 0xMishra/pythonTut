"""
sequence data types
- string -- immutable
- list -- mutable group of elements
- tuple -- immutable group of elements
- set -- unodered collections of unique items, set(mutable), frozenset(immutable)
"""

text = "this is a string"
print(type(text))

list = [1,2,3,4]
print(type(list))

tuple = (1,2,3,4)
print(type(tuple))
